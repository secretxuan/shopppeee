# 前端开发指南

## 📦 快速开始

### 1. 安装依赖

```bash
cd frontend
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

前端将在 http://localhost:3000 启动

### 3. 确保后端运行

前端需要后端 API 支持，请确保后端服务在 http://localhost:8080 运行：

```bash
# 在项目根目录
docker compose up -d
```

## 🎨 设计特点

### 主题色彩

- **主色调**: #336699 (品牌蓝色)
- **成功色**: #52c41a (绿色)
- **警告色**: #faad14 (橙色)
- **错误色**: #ff4d4f (红色)

### 响应式设计

完美适配：
- 📱 手机端 (< 768px)
- 📱 平板端 (768px - 1024px)
- 💻 桌面端 (> 1024px)

### 现代化 UI

- 简洁优雅的卡片式设计
- 流畅的动画过渡效果
- 悬浮效果和阴影
- 圆角设计 (8px)

## 🧩 核心功能

### 1. 用户认证

**注册页面** (`/register`)
- 用户名、邮箱、密码验证
- 密码强度检查
- 表单验证

**登录页面** (`/login`)
- 用户名/密码登录
- JWT 令牌认证
- 自动跳转

**个人中心** (`/profile`)
- 用户信息展示
- 需要登录访问

### 2. 商品浏览

**首页** (`/`)
- 轮播图展示
- 特色服务介绍
- 热门商品推荐

**商品列表** (`/products`)
- 分页展示
- 搜索功能
- 排序功能（价格、时间）
- 响应式网格布局

**商品详情** (`/products/:id`)
- 商品图片展示
- 详细信息
- 库存显示
- 数量选择器
- 加入购物车

### 3. 购物车

**购物车页面** (`/cart`)
- 商品列表展示
- 数量修改
- 删除商品
- 实时计算总价
- 本地持久化存储

## 🔧 技术实现

### 状态管理 (Zustand)

**认证状态** (`useAuthStore`)
```typescript
const { user, isAuthenticated, setAuth, logout } = useAuthStore()
```

**购物车状态** (`useCartStore`)
```typescript
const {
  items,
  addItem,
  removeItem,
  updateQuantity,
  clearCart,
  getTotalPrice,
  getTotalItems
} = useCartStore()
```

### API 调用

**认证 API**
```typescript
import { authAPI } from '@/api/auth'

// 登录
await authAPI.login({ username, password })

// 注册
await authAPI.register({ username, email, password })

// 获取用户信息
await authAPI.getUserInfo()
```

**商品 API**
```typescript
import { productAPI } from '@/api/product'

// 获取商品列表
await productAPI.getProductList({ page, page_size })

// 获取商品详情
await productAPI.getProductById(id)

// 搜索商品
await productAPI.searchProducts(keyword, page, page_size)
```

### 路由配置

```typescript
// 公开路由
/ - 首页
/products - 商品列表
/products/:id - 商品详情
/login - 登录
/register - 注册

// 私有路由（需要登录）
/cart - 购物车
/profile - 个人中心
```

### HTTP 拦截器

**请求拦截器**
- 自动添加 JWT Token
- 设置请求头

**响应拦截器**
- 统一错误处理
- 401 自动跳转登录
- Toast 错误提示

## 📱 组件说明

### Layout 组件

顶部导航栏 + 内容区 + 底部栏的布局结构

**特性**:
- 粘性顶部导航
- 购物车徽章显示数量
- 用户下拉菜单
- 响应式菜单

### ProductCard 组件

商品卡片展示组件

**特性**:
- 悬浮效果
- 缺货遮罩
- 快速加购按钮
- 价格突出显示

### PrivateRoute 组件

私有路由保护组件

**特性**:
- 未登录自动跳转登录页
- 保护需要认证的页面

## 🎯 开发建议

### 1. 组件开发

- 单一职责原则
- 可复用性优先
- 使用 TypeScript 类型
- 添加 PropTypes 验证

### 2. 样式管理

- 使用独立 CSS 文件
- BEM 命名规范
- 响应式设计优先
- 复用 Ant Design 组件样式

### 3. 性能优化

- 使用 React.memo 避免重渲染
- 图片懒加载
- 路由懒加载
- 虚拟列表（长列表）

### 4. 代码规范

```bash
# 代码检查
npm run lint

# 类型检查
npm run type-check
```

## 🐛 常见问题

### Q: API 请求 404

**A**: 检查后端服务是否启动：
```bash
curl http://localhost:8080/health
```

### Q: CORS 错误

**A**: 确保后端 CORS 配置允许 http://localhost:3000

### Q: 登录状态丢失

**A**: 检查浏览器 localStorage 是否被清除

### Q: 购物车数据丢失

**A**: 购物车数据存储在 localStorage，清除缓存会丢失

## 🚀 部署

### 构建生产版本

```bash
npm run build
```

构建产物在 `dist/` 目录

### 预览生产构建

```bash
npm run preview
```

### Nginx 配置示例

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    root /var/www/shoppee/frontend/dist;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    location /api {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## 📚 参考资料

- [React 官方文档](https://react.dev)
- [Ant Design 组件库](https://ant.design)
- [Zustand 状态管理](https://github.com/pmndrs/zustand)
- [Vite 构建工具](https://vitejs.dev)
- [React Router 路由](https://reactrouter.com)

## 🎉 总结

前端采用现代化技术栈，提供优美的用户界面和流畅的交互体验。

**主要特点**:
✅ 响应式设计，完美适配各种设备
✅ 主题色彩统一，视觉效果优美
✅ 组件化开发，代码可维护性强
✅ TypeScript 类型安全
✅ 状态持久化，用户体验好

祝开发愉快！🎨
